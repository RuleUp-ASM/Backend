-- =====================================================================
-- V40: 부정행위 검출 강퇴 → 해당 챌린지 영구 차단 — QA JOIN-11 / SAN-07
--
--  V8 은 "영구 차단 플래그는 두지 않는다 — rejoin_available_at 하나로 전부 표현된다"로 정했지만,
--  방 내부 테크 스펙 5-6(2026-08-31)이 강퇴 3종 중 부정행위 검출만은 백오프가 아니라
--  해당 챌린지 영구 차단(is_permanent=1)으로 확정했다. 가입 게이트는 이 값을 보고
--  409 JOIN_BLOCKED + reason=BANNED 를 내린다(사유는 설명하지 않는다).
--
--  rejoin_available_at 이 null 인 것만으로는 표현할 수 없다 — 대기 없는 재입장(자진 탈퇴 후 대기가
--  끝나 비워진 행, 계정 제재 자동 탈퇴)과 구분되지 않는다. 그래서 별도 컬럼을 둔다.
--
--  재입장(rejoin)으로도 풀리지 않는 값이다 — 풀리려면 운영자가 직접 지워야 한다.
-- =====================================================================

ALTER TABLE `challenge_members`
    ADD COLUMN `rejoin_banned` tinyint(1) NOT NULL DEFAULT 0;
